#ifndef HEAPFORGRAPH_H
#define HEAPFORGRAPH_H

#define InitHeap(heap, capacity, Type) \
    Type elements[capacity];  \
    heap.size = 0;                  \
    heap.elements = elements

#endif //HEAPFORGRAPH_H
